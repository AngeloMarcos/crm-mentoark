import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
} from "@/components/ui/dialog";

export function AccessibilityFix() {
  return null; // Apenas para garantir que DialogTitle seja usado em diálogos genéricos se necessário
}
